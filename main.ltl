// ltl-fasta/main.ltl
//
// FASTA reader / writer with sequence-stats helpers.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-fasta v0.1.0")
    io::println("FASTA reader / writer with sequence-stats helpers.")
    0
}
