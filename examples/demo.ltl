// examples/demo.ltl — runnable demo for ltl_fasta

import std::io
use ltl_fasta as lib

fn main() {
    io::println("ltl_fasta demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}